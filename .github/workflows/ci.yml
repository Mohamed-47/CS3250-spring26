name: CI Test

on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Sanity check
        run: echo "GitHub Actions is running!"
